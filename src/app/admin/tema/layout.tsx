// Override the admin layout padding — the theme editor manages its own full-screen layout
export default function TemaLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
