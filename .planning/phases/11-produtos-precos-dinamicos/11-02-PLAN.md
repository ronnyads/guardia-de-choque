---
id: "11-02"
title: "Reescrever products.ts e pricing.ts para usar banco"
phase: 11
plan: 2
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - src/lib/products.ts
  - src/lib/pricing.ts
autonomous: true
requirements: ["RF-produtos"]
user_setup: []
must_haves:
  truths:
    - "getProductBySlug busca 100% do banco, sem fallback para array local"
    - "getProductsByCategory busca do banco, nao do array local"
    - "getFeaturedProducts busca do banco, nao do array local"
    - "pixPrice e calculado como promo_price * 0.95 (nao usa cost_price)"
    - "storeProducts array removido do products.ts"
    - "KIT_PRICES removido de pricing.ts — calculateExpectedAmount busca do banco"
    - "validateAmount e async e busca preco do Supabase"
  artifacts:
    - path: "src/lib/products.ts"
      provides: "Funcoes de catalogo 100% banco"
    - path: "src/lib/pricing.ts"
      provides: "Validacao de precos dinamica via banco"
  key_links:
    - from: "src/lib/products.ts"
      to: "supabase.from('products')"
      via: "queries com tenant_id"
    - from: "src/lib/pricing.ts"
      to: "supabase.from('products')"
      via: "createServiceSupabase para buscar promo_price por slug"
---

<objective>
Reescrever products.ts para remover o array hardcoded `storeProducts` e buscar tudo do Supabase.
Reescrever pricing.ts para remover `KIT_PRICES` e buscar precos do banco dinamicamente.
Corrigir bug semantico: pixPrice deve ser `promo_price * 0.95`, nao `cost_price`.

Purpose: Sem isso, a loja continua exibindo dados hardcoded mesmo que o admin tenha alterado no banco.
Output: Dois arquivos reescritos, interface StoreProduct mantida identica para nao quebrar consumidores.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/11-produtos-precos-dinamicos/RESEARCH.md
@src/lib/products.ts
@src/lib/pricing.ts
@src/lib/supabase-server.ts
@src/types/tenant.ts

<interfaces>
<!-- Interface StoreProduct que DEVE ser mantida (de src/types/index.ts) -->
```typescript
interface StoreProduct {
  id: string;
  name: string;
  slug: string;
  category: string;
  categoryName: string;
  description: string;
  longDescription: string;
  images: string[];
  price: number;           // promo_price
  originalPrice: number;   // original_price
  pixPrice: number;        // CORRIGIR: promo_price * 0.95 (NAO cost_price)
  installments: { count: number; value: number };
  rating: number;
  reviewCount: number;
  badge: string;
  inStock: boolean;
  features: Array<{ icon: string; title: string; description: string }>;
  specs: Array<{ label: string; value: string }>;
  quantity?: number;
}
```

<!-- Consumidores de products.ts que NAO devem quebrar -->
- src/app/loja/page.tsx — usa storeProducts direto (ProductGrid) e getFeaturedProducts
- src/app/produto/[slug]/page.tsx — usa getProductBySlug, generateStaticParams
- src/app/categoria/[slug]/page.tsx — usa getProductsByCategory
- src/components/home/ProductScroll.tsx — usa storeProducts direto (client component)
- src/lib/products.ts — getRelatedProducts (ja busca do banco, manter)

<!-- Consumidores de pricing.ts que NAO devem quebrar -->
- src/app/api/checkout/pix/route.ts — chama validateAmount(amount, params)
- src/app/api/checkout/card/route.ts — chama validateAmount(amount, params)
- src/app/api/checkout/stripe-card/route.ts — chama validateAmount(amount, params)

<!-- createServiceSupabase (de supabase-server.ts) — para bypass RLS no pricing -->
```typescript
import { createServiceSupabase } from '@/lib/supabase-server';
// Retorna supabase client com service role key
```

<!-- supabase client-side (de src/lib/supabase.ts) — usado em products.ts atual -->
```typescript
import { supabase } from './supabase';
// Client-side supabase, sujeito a RLS
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reescrever products.ts — remover hardcodes, tudo do banco</name>
  <files>src/lib/products.ts</files>
  <action>
  Reescrever `src/lib/products.ts` completamente. Remover o array `storeProducts` e todas as funcoes que o usam. Substituir por funcoes async que buscam do Supabase.

  **Funcao helper de mapeamento (interna):**
  ```typescript
  import { StoreProduct } from "@/types";
  import { supabase } from "./supabase";

  const PIX_DISCOUNT = 0.05;

  function mapDbToStoreProduct(d: Record<string, unknown>): StoreProduct {
    const promoPrice = Number(d.promo_price) || 0;
    return {
      id: String(d.id),
      name: String(d.name),
      slug: String(d.slug),
      category: String(d.category_id || ''),
      categoryName: String(d.category_id || 'Categoria').replace(/-/g, ' ').replace(/\b\w/g, c => c.toUpperCase()),
      description: String(d.description || ''),
      longDescription: String(d.long_description || d.description || ''),
      images: Array.isArray(d.images) ? d.images as string[] : [],
      price: promoPrice,
      originalPrice: Number(d.original_price) || promoPrice,
      pixPrice: Math.round(promoPrice * (1 - PIX_DISCOUNT) * 100) / 100,
      installments: { count: 6, value: Math.round((promoPrice / 6) * 100) / 100 },
      rating: Number(d.rating) || 0,
      reviewCount: Number(d.review_count) || 0,
      badge: String(d.badge || ''),
      inStock: d.status === 'active',
      features: Array.isArray(d.features) ? d.features : [],
      specs: Array.isArray(d.specs) ? d.specs : [],
      quantity: Number(d.quantity) || undefined,
    };
  }
  ```

  **Funcoes exportadas (todas async agora):**

  ```typescript
  export async function getProductBySlug(slug: string): Promise<StoreProduct | undefined> {
    const { data, error } = await supabase
      .from("products")
      .select("*")
      .eq("slug", slug)
      .eq("status", "active")
      .single();
    if (error || !data) return undefined;
    return mapDbToStoreProduct(data);
  }

  export async function getProductsByCategory(categorySlug: string): Promise<StoreProduct[]> {
    const { data } = await supabase
      .from("products")
      .select("*")
      .eq("category_id", categorySlug)
      .eq("status", "active")
      .order("created_at", { ascending: false });
    return (data ?? []).map(mapDbToStoreProduct);
  }

  export async function getFeaturedProducts(): Promise<StoreProduct[]> {
    const { data } = await supabase
      .from("products")
      .select("*")
      .eq("status", "active")
      .order("created_at", { ascending: false })
      .limit(10);
    return (data ?? []).map(mapDbToStoreProduct);
  }

  export async function getAllProductSlugs(): Promise<string[]> {
    const { data } = await supabase
      .from("products")
      .select("slug")
      .eq("status", "active");
    return (data ?? []).map(d => d.slug);
  }
  ```

  **Manter `getRelatedProducts` como esta** (ja busca do banco), mas usar `mapDbToStoreProduct` para corrigir o bug do pixPrice nela tambem.

  **IMPORTANTE:**
  - pixPrice = `promo_price * 0.95` (NAO `cost_price` — bug corrigido)
  - categoryName: derivar de category_id fazendo capitalize (ex: "defesa-pessoal" -> "Defesa Pessoal")
  - Nao usar `storeProducts` como fallback em lugar nenhum
  - `getProductsByCategory` e `getFeaturedProducts` agora sao async (antes eram sync)
  - Exportar `getAllProductSlugs` para uso em `generateStaticParams`

  **Atentar para consumidores que vao quebrar por causa da mudanca sync->async:**
  - `src/app/loja/page.tsx` — se usa `storeProducts` direto, precisara usar `await getFeaturedProducts()`
  - `src/app/categoria/[slug]/page.tsx` — se usa `getProductsByCategory` sync, precisara `await`
  - `src/components/home/ProductScroll.tsx` — client component que importa `storeProducts` — este arquivo sera tratado no plan 11-04. POR ENQUANTO, exportar um array vazio como fallback temporario:
    ```typescript
    /** @deprecated — Usar getFeaturedProducts() async. Sera removido no plan 11-04. */
    export const storeProducts: StoreProduct[] = [];
    ```
  - `src/app/produto/[slug]/page.tsx` — se usa `generateStaticParams` com storeProducts, trocar para `getAllProductSlugs`. Esse arquivo ja usa `getProductBySlug` async, nao quebra.

  Ajustar os consumidores diretos que importam funcoes de products.ts para usar await:
  - `src/app/loja/page.tsx` — trocar `storeProducts` por `await getFeaturedProducts()` 
  - `src/app/categoria/[slug]/page.tsx` — trocar `getProductsByCategory(slug)` por `await getProductsByCategory(slug)`
  - `src/app/produto/[slug]/page.tsx` — trocar `generateStaticParams` para usar `await getAllProductSlugs()` retornando `slugs.map(slug => ({ slug }))`
  </action>
  <verify>
    <automated>cd c:/Users/euron/Documents/guardia-de-choque && rtk npx tsc --noEmit 2>&1 | head -50</automated>
  </verify>
  <done>
  - storeProducts array removido (so existe export deprecated vazio para ProductScroll)
  - getProductBySlug, getProductsByCategory, getFeaturedProducts buscam do Supabase
  - pixPrice = promo_price * 0.95 em todos os mapeamentos
  - Paginas loja, categoria e produto compilam sem erros com as funcoes async
  - getAllProductSlugs exportado para generateStaticParams
  </done>
</task>

<task type="auto">
  <name>Task 2: Reescrever pricing.ts — validacao dinamica de precos</name>
  <files>src/lib/pricing.ts</files>
  <action>
  Reescrever `src/lib/pricing.ts` para buscar precos do banco em vez de usar `KIT_PRICES` hardcoded.

  **Mudancas:**

  1. **Remover** `KIT_PRICES` (o objeto literal com precos hardcoded).

  2. **Adicionar funcao de lookup no banco:**
  ```typescript
  import { createServiceSupabase } from '@/lib/supabase-server';

  async function getProductPrice(slug: string): Promise<number> {
    const supabase = createServiceSupabase();
    const { data, error } = await supabase
      .from('products')
      .select('promo_price')
      .eq('slug', slug)
      .eq('status', 'active')
      .single();
    if (error || !data) throw new Error(`Produto nao encontrado: ${slug}`);
    return Number(data.promo_price);
  }
  ```

  3. **Tornar `calculateExpectedAmount` async:**
  ```typescript
  export async function calculateExpectedAmount(p: PriceParams): Promise<number> {
    const base = await getProductPrice(p.kitId);
    let total = base;
    if (p.hasBump)   total += ORDER_BUMP_PRICE;
    if (p.hasUpsell) total += UPSELL_PRICE;
    if (p.paymentMethod === "pix") total = total * (1 - PIX_DISCOUNT);
    return Math.round(total * 100) / 100;
  }
  ```

  4. **Tornar `validateAmount` async:**
  ```typescript
  export async function validateAmount(clientAmount: number, params: PriceParams): Promise<void> {
    const expected = await calculateExpectedAmount(params);
    const diff = Math.abs(clientAmount - expected);
    if (diff > AMOUNT_TOLERANCE) {
      console.warn(`[Security] Amount mismatch: client=${clientAmount} expected=${expected} diff=${diff}`, params);
      throw new Error("Valor do pedido invalido. Recarregue a pagina e tente novamente.");
    }
  }
  ```

  5. **Manter intactas** as funcoes de sanitize (`sanitizeString`, `sanitizeEmail`, `sanitizeDocument`, `sanitizeAmount`) — nao tocar nelas.

  6. **Manter constantes** `ORDER_BUMP_PRICE`, `UPSELL_PRICE`, `PIX_DISCOUNT`, `AMOUNT_TOLERANCE` — essas ficam hardcoded por enquanto (sao constantes de negocio, nao de catalogo).

  7. **Manter** a interface `PriceParams` — nao muda.

  **IMPORTANTE:** Como `validateAmount` agora e async, os consumidores precisam usar `await`. Isso sera tratado no plan 11-03. Este plan so muda pricing.ts — os callers vao ter erro de tipo ate o plan 11-03 rodar. Isso e esperado e aceitavel.
  </action>
  <verify>
    <automated>cd c:/Users/euron/Documents/guardia-de-choque && rtk npx tsc --noEmit --noUnusedLocals false 2>&1 | grep -E "pricing|route" | head -20</automated>
  </verify>
  <done>
  - KIT_PRICES removido de pricing.ts
  - calculateExpectedAmount busca preco do banco via createServiceSupabase
  - validateAmount e async
  - Funcoes de sanitize intactas
  - Constantes ORDER_BUMP_PRICE, UPSELL_PRICE, PIX_DISCOUNT mantidas
  - Erros de tipo esperados nos checkout routes (serao corrigidos no plan 11-03)
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — Podem existir erros SOMENTE nos arquivos de checkout (pix/route.ts, card/route.ts, stripe-card/route.ts) por causa da mudanca sync->async de validateAmount. Erros em products.ts ou pricing.ts = falha.
2. Grep para confirmar que `storeProducts` nao e mais usado como fonte de dados (so o export deprecated vazio)
3. Grep para confirmar que `KIT_PRICES` nao existe mais em pricing.ts
</verification>

<success_criteria>
- Zero referencia a dados hardcoded em products.ts (exceto export vazio deprecated)
- Zero referencia a KIT_PRICES em pricing.ts
- pixPrice calculado como promo_price * 0.95 em todas as funcoes de mapeamento
- validateAmount e async e busca do banco
- Paginas loja, categoria e produto usam funcoes async corretamente
</success_criteria>

<output>
After completion, create `.planning/phases/11-produtos-precos-dinamicos/11-02-SUMMARY.md`
</output>
