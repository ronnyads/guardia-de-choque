---
id: "11-04"
title: "Limpar hardcodes restantes e build final"
phase: 11
plan: 4
type: execute
wave: 4
depends_on: ["11-02", "11-03"]
files_modified:
  - src/lib/constants.ts
  - src/components/product/ProductInfo.tsx
  - src/components/home/ProductScroll.tsx
  - src/components/home/HeroSection.tsx
  - src/components/sections/KitOffers.tsx
autonomous: true
requirements: ["RF-produtos"]
user_setup: []
must_haves:
  truths:
    - "KIT_VARIANTS removido de ProductInfo.tsx — seletor de variantes busca dados do produto/banco"
    - "KITS removido ou nao mais usado como fonte de precos/dados de produto"
    - "storeProducts deprecated export removido de products.ts"
    - "ProductScroll recebe produtos como prop em vez de importar array"
    - "next build compila com 0 erros e 0 warnings relevantes"
  artifacts:
    - path: "src/components/product/ProductInfo.tsx"
      provides: "Seletor de variantes dinamico"
    - path: "src/components/home/ProductScroll.tsx"
      provides: "Scroll de produtos via props (nao hardcoded)"
    - path: "src/lib/constants.ts"
      provides: "Sem dados de produto/preco (KITS, MAIN_PRODUCT podem ser removidos)"
  key_links:
    - from: "src/components/product/ProductInfo.tsx"
      to: "props.variants ou dados do banco"
      via: "array de variantes passado como prop pela pagina pai"
    - from: "src/components/home/ProductScroll.tsx"
      to: "props.products"
      via: "pagina pai (server component) busca e passa"
---

<objective>
Limpar todos os hardcodes restantes de produtos e precos que ainda existem no codebase.
Os 4 focos de hardcode identificados na research: storeProducts (plan 11-02 removeu parcialmente),
KITS/constants.ts, KIT_VARIANTS/ProductInfo.tsx, e qualquer outro import residual.
Ao final, `next build` deve compilar com 0 erros.

Purpose: Garantir que o catalogo e 100% dinamico — qualquer mudanca no admin reflete na loja.
Output: Build limpo. Zero dados de produto/preco hardcoded no frontend.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/11-produtos-precos-dinamicos/RESEARCH.md
@src/lib/constants.ts
@src/components/product/ProductInfo.tsx
@src/components/home/ProductScroll.tsx

<interfaces>
<!-- StoreProduct interface (mantida desde plan 11-02) -->
```typescript
interface StoreProduct {
  id: string;
  name: string;
  slug: string;
  category: string;
  categoryName: string;
  description: string;
  longDescription: string;
  images: string[];
  price: number;
  originalPrice: number;
  pixPrice: number;
  installments: { count: number; value: number };
  rating: number;
  reviewCount: number;
  badge: string;
  inStock: boolean;
  features: Array<{ icon: string; title: string; description: string }>;
  specs: Array<{ label: string; value: string }>;
  quantity?: number;
}
```

<!-- Funcoes disponiveis de products.ts (pos plan 11-02) -->
```typescript
export async function getFeaturedProducts(): Promise<StoreProduct[]>;
export async function getProductBySlug(slug: string): Promise<StoreProduct | undefined>;
export async function getProductsByCategory(categorySlug: string): Promise<StoreProduct[]>;
export async function getAllProductSlugs(): Promise<string[]>;
export async function getRelatedProducts(slug: string, limit?: number): Promise<StoreProduct[]>;
// DEPRECATED (sera removido neste plan):
export const storeProducts: StoreProduct[] = [];
```

<!-- Consumidores de KITS (constants.ts) que precisam migrar -->
- src/components/home/HeroSection.tsx — KITS[1] para exibir preco do kit dupla
- src/components/sections/KitOffers.tsx — KITS para renderizar cards de kit
- src/app/checkout/ — ClientCheckout pode usar KITS (verificar)

<!-- ProductScroll (client component) -->
```typescript
// Atualmente importa storeProducts direto
// Precisa receber como prop da pagina pai (server component)
```

<!-- KIT_VARIANTS em ProductInfo.tsx -->
```typescript
const KIT_VARIANTS = [
  { slug: "guardia-de-choque", label: "Individual", qty: "1 aparelho", price: 97.90, highlight: false },
  { slug: "kit-dupla", label: "Dupla Protecao", qty: "2 aparelhos", price: 169.90, highlight: true },
  { slug: "kit-familia", label: "Kit Familia", qty: "3 aparelhos", price: 227.90, highlight: false },
];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remover KIT_VARIANTS de ProductInfo.tsx — variantes via prop</name>
  <files>src/components/product/ProductInfo.tsx, src/app/produto/[slug]/page.tsx</files>
  <action>
  **Em ProductInfo.tsx:**

  1. Remover a constante `KIT_VARIANTS` hardcoded (linhas 17-21).

  2. Adicionar ao interface `Props`:
  ```typescript
  interface Props {
    product: StoreProduct;
    variants?: Array<{ slug: string; label: string; qty: string; price: number; highlight: boolean }>;
    onAdd?: () => void;
  }
  ```

  3. No componente, receber `variants` das props:
  ```typescript
  export default function ProductInfo({ product, variants = [], onAdd }: Props) {
  ```

  4. No JSX onde renderiza o seletor de kit (o `grid grid-cols-3` com KIT_VARIANTS.map), condicionar a renderizacao:
  ```typescript
  {variants.length > 0 && (
    <div className="flex flex-col gap-2 pb-3 border-b border-[#E2E8F0]">
      <p className="text-[11px] font-semibold text-[#94A3B8] uppercase tracking-widest">Escolha seu kit</p>
      <div className="grid grid-cols-3 gap-2">
        {variants.map((v) => {
          const isActive = product.slug === v.slug;
          return (
            // ... mesmo JSX que ja existe, usando v.slug, v.label, v.qty, v.price, v.highlight
          );
        })}
      </div>
    </div>
  )}
  ```

  **Em src/app/produto/[slug]/page.tsx:**

  Na pagina de produto (server component), buscar os variants do banco e passar como prop:

  ```typescript
  // Dentro da funcao do page, apos buscar o produto:
  const allProducts = await getFeaturedProducts();
  const variants = allProducts
    .filter(p => p.category === product.category && p.slug !== 'mini-taser')
    .map(p => ({
      slug: p.slug,
      label: p.name.replace('Guardia de Choque — ', '').replace('Kit ', ''),
      qty: p.quantity ? `${p.quantity} aparelho${p.quantity > 1 ? 's' : ''}` : '1 aparelho',
      price: p.price,
      highlight: p.badge === 'Kit' || p.slug === 'kit-dupla',
    }));

  // Passar para o componente:
  <ProductInfo product={product} variants={variants} />
  ```

  Se `produto/[slug]/page.tsx` nao importa `getFeaturedProducts`, adicionar o import. Se ja importa `getProductBySlug`, esta no mesmo arquivo.
  </action>
  <verify>
    <automated>cd c:/Users/euron/Documents/guardia-de-choque && rtk npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
  - KIT_VARIANTS removido de ProductInfo.tsx
  - Seletor de variantes renderiza a partir de props dinâmicas
  - Pagina de produto busca variants do banco e passa como prop
  - TypeScript compila sem erros
  </done>
</task>

<task type="auto">
  <name>Task 2: Migrar ProductScroll, HeroSection, KitOffers + limpar constants.ts + build final</name>
  <files>src/components/home/ProductScroll.tsx, src/components/home/HeroSection.tsx, src/components/sections/KitOffers.tsx, src/lib/constants.ts, src/lib/products.ts</files>
  <action>
  **Passo 1: Identificar todos os consumidores restantes de hardcodes**

  Antes de qualquer mudanca, rodar:
  ```bash
  grep -rn "storeProducts\|import.*constants\|from.*constants\|KITS\[" src/ --include="*.tsx" --include="*.ts" | grep -v node_modules | grep -v ".next"
  ```
  
  Listar todos os arquivos que importam `storeProducts` de products.ts ou `KITS`/`MAIN_PRODUCT` de constants.ts. Tratar cada um.

  **Passo 2: ProductScroll.tsx (client component)**

  Este e um client component que provavelmente importa `storeProducts` direto. Mudar para receber via props:

  1. Adicionar interface Props: `interface Props { products: StoreProduct[] }`
  2. Mudar assinatura: `export default function ProductScroll({ products }: Props)`
  3. Remover import de `storeProducts` de products.ts
  4. Na pagina pai que usa ProductScroll (provavelmente `src/app/page.tsx` ou similar — server component), buscar produtos e passar como prop:
  ```typescript
  const products = await getFeaturedProducts();
  <ProductScroll products={products} />
  ```

  **Passo 3: HeroSection.tsx**

  Se importa `KITS[1]` para exibir preco do kit dupla, mudar para receber como prop:
  1. Adicionar prop `highlightPrice?: number` (ou similar)
  2. Pagina pai busca e passa
  3. Remover import de KITS

  OU, se HeroSection esta numa page que ja tem os dados, simplesmente passar como prop.

  **Passo 4: KitOffers.tsx**

  Se importa `KITS` para renderizar cards, mudar para receber como prop:
  1. Adicionar interface Props com `kits: Kit[]` ou `products: StoreProduct[]`
  2. Pagina pai busca do banco e passa
  3. Remover import de KITS

  **Passo 5: Limpar constants.ts**

  Apos migrar todos os consumidores:
  - Remover `KITS` array
  - Remover `MAIN_PRODUCT` e `MINI_TASER` (dados de produto hardcoded)
  - MANTER: `REVIEWS`, `FAQ_ITEMS`, `STATS`, `CHECKOUT_URLS` — esses NAO sao dados de produto/preco, sao conteudo de pagina (serao migrados em outra fase)
  - Se `KITS` era o unico export relevante para checkout, verificar se `ClientCheckout.tsx` usa — se sim, migrar para buscar do banco via API ou prop

  **Passo 6: Remover export deprecated de products.ts**

  Em `src/lib/products.ts`, remover:
  ```typescript
  /** @deprecated */
  export const storeProducts: StoreProduct[] = [];
  ```

  **Passo 7: Build final**
  ```bash
  npx next build
  ```

  Corrigir quaisquer erros restantes. O objetivo e 0 erros de build. Warnings de "missing image optimization" sao aceitaveis.

  **IMPORTANTE:** Cada arquivo que importava dados hardcoded precisa ser verificado individualmente. Use o grep do Passo 1 como checklist. Nao deixar nenhum import orfao.
  </action>
  <verify>
    <automated>cd c:/Users/euron/Documents/guardia-de-choque && rtk next build 2>&1 | tail -20</automated>
  </verify>
  <done>
  - storeProducts export removido de products.ts
  - KITS, MAIN_PRODUCT, MINI_TASER removidos de constants.ts
  - ProductScroll recebe produtos via props
  - HeroSection nao usa KITS hardcoded
  - KitOffers nao usa KITS hardcoded
  - `next build` compila com 0 erros
  - Zero dados de produto/preco hardcoded no codebase (exceto REVIEWS, FAQ_ITEMS, STATS, CHECKOUT_URLS que sao conteudo de pagina)
  </done>
</task>

</tasks>

<verification>
1. `next build` — 0 erros, build completo
2. `grep -rn "KIT_PRICES\|KIT_VARIANTS\|storeProducts" src/ --include="*.ts" --include="*.tsx"` — zero resultados (exceto comments se houver)
3. `grep -rn "KITS\b" src/ --include="*.ts" --include="*.tsx"` — zero imports restantes de KITS como dado de produto
4. Verificar que constants.ts so contem REVIEWS, FAQ_ITEMS, STATS, CHECKOUT_URLS (conteudo de pagina, nao produto)
</verification>

<success_criteria>
- `next build` passa com 0 erros
- Zero hardcodes de produtos/precos no frontend
- Todos os componentes que exibiam dados de produto agora recebem via props ou buscam do banco
- constants.ts limpo de dados de produto (KITS, MAIN_PRODUCT, MINI_TASER removidos)
- ProductInfo.tsx usa variants dinamicos via prop
- ProductScroll usa products via prop
</success_criteria>

<output>
After completion, create `.planning/phases/11-produtos-precos-dinamicos/11-04-SUMMARY.md`
</output>
