---
id: "11-03"
title: "Checkout Dinamico — validateAmount async do banco"
phase: 11
plan: 3
type: execute
wave: 3
depends_on: ["11-02"]
files_modified:
  - src/app/api/checkout/pix/route.ts
  - src/app/api/checkout/card/route.ts
  - src/app/api/checkout/stripe-card/route.ts
autonomous: true
requirements: ["RF-produtos"]
user_setup: []
must_haves:
  truths:
    - "Checkout PIX valida preco do banco (nao hardcoded)"
    - "Checkout Card valida preco do banco (nao hardcoded)"
    - "Checkout Stripe valida preco do banco (nao hardcoded)"
    - "Se preco mudar no admin, checkout usa o preco novo na proxima validacao"
  artifacts:
    - path: "src/app/api/checkout/pix/route.ts"
      provides: "Rota PIX com validateAmount async"
    - path: "src/app/api/checkout/card/route.ts"
      provides: "Rota Card com validateAmount async"
    - path: "src/app/api/checkout/stripe-card/route.ts"
      provides: "Rota Stripe com validateAmount async"
  key_links:
    - from: "src/app/api/checkout/pix/route.ts"
      to: "src/lib/pricing.ts"
      via: "await validateAmount(amount, params)"
    - from: "src/app/api/checkout/card/route.ts"
      to: "src/lib/pricing.ts"
      via: "await validateAmount(amount, params)"
    - from: "src/app/api/checkout/stripe-card/route.ts"
      to: "src/lib/pricing.ts"
      via: "await validateAmount(amount, params)"
---

<objective>
Atualizar as 3 rotas de checkout para usar `await validateAmount()` que agora e async (mudado no plan 11-02).
A unica mudanca necessaria e adicionar `await` na chamada de validateAmount em cada rota.

Purpose: Sem isso, a validacao anti-fraude nao funciona (validateAmount retorna Promise, nao valida nada).
Output: 3 rotas de checkout funcionais com validacao de preco dinamica do banco.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/11-produtos-precos-dinamicos/RESEARCH.md
@src/app/api/checkout/pix/route.ts
@src/app/api/checkout/card/route.ts
@src/app/api/checkout/stripe-card/route.ts
@src/lib/pricing.ts

<interfaces>
<!-- Interface de pricing.ts apos plan 11-02 -->
```typescript
// validateAmount agora e async:
export async function validateAmount(clientAmount: number, params: PriceParams): Promise<void>;

// PriceParams nao mudou:
export interface PriceParams {
  kitId: string;
  hasBump: boolean;
  hasUpsell: boolean;
  paymentMethod: "pix" | "cartao";
}

// Sanitize functions NAO mudaram (sync):
export function sanitizeString(val: unknown, maxLen?: number): string;
export function sanitizeEmail(val: unknown): string;
export function sanitizeDocument(val: unknown): string;
export function sanitizeAmount(val: unknown): number;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Adicionar await a validateAmount nas 3 rotas de checkout</name>
  <files>src/app/api/checkout/pix/route.ts, src/app/api/checkout/card/route.ts, src/app/api/checkout/stripe-card/route.ts</files>
  <action>
  Em cada uma das 3 rotas, fazer a mesma mudanca minima:

  **Antes (em cada rota):**
  ```typescript
  validateAmount(amount, {
    kitId:         String(body.kitId || ""),
    hasBump:       Boolean(body.hasBump),
    hasUpsell:     Boolean(body.hasUpsell),
    paymentMethod: "pix",  // ou "cartao" nas rotas de card
  });
  ```

  **Depois:**
  ```typescript
  await validateAmount(amount, {
    kitId:         String(body.kitId || ""),
    hasBump:       Boolean(body.hasBump),
    hasUpsell:     Boolean(body.hasUpsell),
    paymentMethod: "pix",  // ou "cartao" nas rotas de card
  });
  ```

  A funcao POST ja e async em todas as 3 rotas, entao adicionar `await` e suficiente.

  **Rotas e seus paymentMethod:**
  - `pix/route.ts` — paymentMethod: `"pix"` (ja esta correto)
  - `card/route.ts` — paymentMethod: `"cartao"` (ja esta correto)
  - `stripe-card/route.ts` — paymentMethod: `"cartao"` (ja esta correto)

  **Nao mudar mais nada nas rotas.** O import de `validateAmount` ja existe. As funcoes de sanitize continuam sync. So o `await` precisa ser adicionado.
  </action>
  <verify>
    <automated>cd c:/Users/euron/Documents/guardia-de-choque && rtk npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
  - As 3 rotas de checkout usam `await validateAmount(...)` 
  - TypeScript compila sem erros em todos os arquivos de checkout
  - A cadeia completa funciona: checkout POST -> await validateAmount -> getProductPrice do banco -> valida ou rejeita
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — 0 erros em todos os arquivos (agora que todos os callers de validateAmount usam await)
2. Grep confirma: `grep -r "validateAmount" src/app/api/checkout/` — todas as ocorrencias tem `await` antes
</verification>

<success_criteria>
- TypeScript compila sem erros em todas as rotas de checkout
- Nenhuma chamada a validateAmount sem await
- Fluxo completo: cliente envia amount -> rota busca preco do banco -> compara -> aceita ou rejeita
</success_criteria>

<output>
After completion, create `.planning/phases/11-produtos-precos-dinamicos/11-03-SUMMARY.md`
</output>
