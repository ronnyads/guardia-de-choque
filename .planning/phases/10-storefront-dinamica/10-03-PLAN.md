---
id: "10-03"
title: "Components Dinâmicos — Navbar + Footer"
phase: 10
wave: 3
depends_on: ["10-01", "10-02"]
files_modified:
  - src/components/layout/Navbar.tsx
  - src/components/layout/Footer.tsx
autonomous: true
requirements: ["RF-02"]
user_setup: []

must_haves:
  truths:
    - "Navbar exibe config.brand_name do banco (não 'Os Oliveiras' hardcoded)"
    - "Navbar exibe config.announcement_messages do banco (não STRIP_ITEMS hardcoded)"
    - "Navbar usa useStoreConfig() para ler dados dinamicamente"
    - "Footer exibe config.brand_name do banco"
    - "Fallback para valores default funciona quando config está vazia"
    - "Loja abre sem erros de console"
    - "npx tsc --noEmit passa com 0 erros"
    - "rtk next build passa com 0 erros"
  artifacts: []
  key_links:
    - "Navbar.tsx usa useStoreConfig() — é Client Component, já tem 'use client'"
    - "Footer.tsx pode ser Server ou Client — se Client, usa useStoreConfig()"
    - "announcement_messages fallback: array vazio mostra nada (comportamento correto)"
    - "brand_name fallback: 'Minha Loja' (do DEFAULT_CONFIG em store-config.ts)"
---

<objective>
Tornar Navbar e Footer dinâmicos: nome da marca e mensagens de anúncio lidos do banco via TenantProvider.

Purpose: Estas são as partes mais visíveis da loja — quando o lojista muda o nome no admin, deve aparecer imediatamente.

Output: Navbar e Footer sem strings hardcoded de marca, compatíveis com qualquer tenant.
</objective>

## Contexto

Projeto: c:\Users\euron\Documents\guardia-de-choque

**Leia AMBOS os arquivos antes de modificar:**
- `src/components/layout/Navbar.tsx`
- `src/components/layout/Footer.tsx`

**Leia também:**
- `src/components/providers/TenantProvider.tsx` (para ver a interface do hook)

## TASK 1 — Atualizar src/components/layout/Navbar.tsx

O Navbar é um Client Component (`'use client'`). Padrão de mudança:

```typescript
// ADICIONAR import
import { useStoreConfig } from '@/components/providers/TenantProvider';

// DENTRO do componente, após os outros hooks:
export default function Navbar() {
  const config = useStoreConfig();

  // SUBSTITUIR STRIP_ITEMS hardcoded por:
  const announcements = config.announcement_messages.length > 0
    ? config.announcement_messages
    : ["🔒 Compra 100% segura"]; // fallback mínimo

  // SUBSTITUIR "Os Oliveiras" hardcoded por:
  const brandName = config.brand_name ?? 'Minha Loja';

  // ... resto do componente usa announcements e brandName
}
```

**REGRAS:**
1. Leia o arquivo atual completo antes de editar
2. Mantenha TODA a estrutura visual existente (animações, estilos, links)
3. Só mude: a constante STRIP_ITEMS (substituída por announcements) e o brand name literal
4. NÃO altere navLinks, cores, layout ou qualquer outra lógica

## TASK 2 — Atualizar src/components/layout/Footer.tsx

Leia o arquivo atual. Se for Server Component (sem `'use client'`):

```typescript
// Opção A — Server Component: receber config como prop do layout
// (verificar como o layout atual passa props para o footer)

// Opção B — Converter para Client Component e usar useStoreConfig():
'use client';
import { useStoreConfig } from '@/components/providers/TenantProvider';

export default function Footer() {
  const config = useStoreConfig();
  const brandName = config.brand_name ?? 'Minha Loja';
  const year = new Date().getFullYear();
  // usar brandName em vez do valor hardcoded
}
```

**Escolha a opção mais simples** baseado no que o arquivo atual já faz. Se já é Client Component, use `useStoreConfig()`. Se é Server Component, converta para Client (é seguro para um footer).

**REGRAS:**
1. Leia o arquivo atual completo antes de editar
2. Mantenha TODA a estrutura visual e links existentes
3. Só mude: o nome da marca hardcoded → brandName da config

## TASK 3 — Verificar se há outros componentes com marca hardcoded

Fazer busca rápida:
```bash
grep -r "Os Oliveiras\|Guardiã de Choque\|guardia-de-choque" src/components/ --include="*.tsx" -l
```

Para cada arquivo encontrado: avaliar se é uma referência que deve ser dinâmica (marca, slogan) ou uma referência de produto/conteúdo que fica para a Fase 11.

- Referências de **marca** (nome da loja) → tornar dinâmico com `config.brand_name`
- Referências de **produto** (nome do produto, preço) → manter como está (Fase 11)

## TASK 4 — Build final

```bash
npx tsc --noEmit
rtk next build
```

Build DEVE passar com 0 erros. Deploy não é necessário neste plano (será feito pelo orchestrador após completar a fase).
